---
"pdfn": patch
---

Add underscore prefix convention to exclude files from template list. Files starting with `_` (e.g., `_utils.tsx`, `_shared.tsx`) are now excluded from the preview sidebar, similar to how subdirectories are excluded.
